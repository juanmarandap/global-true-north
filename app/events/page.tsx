import Events from "@/components/Events";

export const metadata = { title: "Eventos — Global True North" };

export default function EventsPage() {
  return (
    <div className="pt-[73px]">
      <Events />
    </div>
  );
}
